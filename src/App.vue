<script setup>
import { RouterView } from 'vue-router'
</script>

<template>
  <div>
    <nav>
      <RouterLink to="/client">Cadastrar</RouterLink>
      <RouterLink to="/list">Listar</RouterLink>
      <RouterLink to="/retrieve">Encontrar</RouterLink>
      <RouterLink to="/update">Editar</RouterLink>
    </nav>
  </div>

  <RouterView />
</template>

<style scoped>
div {
  display: flex;
  align-items: flex-start;
  justify-content: flex-start;
}
header {
  line-height: 1.5;
  max-height: 100vh;
}

nav {
  width: 200px;
  font-size: 12px;
  text-align: center;
  margin-top: 2rem;
  border: 1px solid;
  border-radius: 6px;
}

nav a.router-link-exact-active {
  color: var(--color-text);
}

nav a.router-link-exact-active:hover {
  background-color: transparent;
}

nav a {
  display: inline-block;
  padding: 0 1rem;
  border-left: 1px solid var(--color-border);
}

nav a:first-of-type {
  border: 0;
}

@media (min-width: 1024px) {
  header {
    display: flex;
    place-items: center;
    padding-right: calc(var(--section-gap) / 2);
  }

  .logo {
    margin: 0 2rem 0 0;
  }

  header .wrapper {
    display: flex;
    place-items: flex-start;
    flex-wrap: wrap;
  }

  nav {
    margin-left: -1rem;
    font-size: 1rem;

    padding: 1rem 0;
    margin-top: 1rem;
  }
}
</style>
